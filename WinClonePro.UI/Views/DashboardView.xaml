<UserControl x:Class="WinClonePro.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Text="Dashboard" />

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Margin="0,0,8,0"
                           VerticalAlignment="Center"
                           Text="Theme" />

                <ComboBox Width="130"
                          Margin="0,0,12,0"
                          ItemsSource="{Binding ThemeModes}"
                          SelectedItem="{Binding SelectedThemeMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button Margin="0,0,12,0"
                        Command="{Binding RefreshDisksCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Content="Refresh" />

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ProgressBar Width="120"
                                 Height="6"
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
                </StackPanel>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DataGrid ItemsSource="{Binding Disks}"
                      SelectedItem="{Binding SelectedDisk, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      RowHeaderWidth="0"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      materialDesign:DataGridAssist.CellPadding="8"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBootDisk}" Value="True">
                                <Setter Property="Background" Value="#33FFB300"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="70"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding FriendlySize}" Width="110"/>
                    <DataGridTextColumn Header="Drives" Binding="{Binding DriveLettersDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding MediaType}" Width="110"/>
                    <DataGridTextColumn Header="Health" Binding="{Binding HealthBadge}" Width="110"/>
                    <DataGridTemplateColumn Header="OS" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="6,2"
                                        CornerRadius="10"
                                        Background="#2233CC33"
                                        Visibility="{Binding ContainsOS, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="OS DETECTED"
                                               FontWeight="Bold"
                                               Foreground="#FF1B5E20"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Boot" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="6,2"
                                        CornerRadius="10"
                                        Background="#22FFB300"
                                        Visibility="{Binding IsBootDisk, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="BOOT"
                                               FontWeight="Bold"
                                               Foreground="#FF8D6E00"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="1"
                    Margin="0,12,0,0"
                    Padding="12"
                    Background="{DynamicResource MaterialDesignPaper}"
                    CornerRadius="8"
                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                    BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock FontWeight="SemiBold" Text="Selected disk" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding SelectedDisk.Model, TargetNullValue=''}" />
                        <TextBlock Text="{Binding SelectedDisk.SerialNumber, TargetNullValue=''}" Opacity="0.8"/>
                        <TextBlock Margin="0,8,0,0" Text="{Binding SelectedDisk.FriendlySize, TargetNullValue=''}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock FontWeight="SemiBold" Text="Details" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding SelectedDisk.Index, StringFormat=Index: {0}}"/>
                        <TextBlock Text="{Binding SelectedDisk.MediaType, StringFormat=MediaType: {0}, TargetNullValue=''}"/>
                        <TextBlock Text="{Binding SelectedDisk.Status, StringFormat=Status: {0}, TargetNullValue=''}"/>
                        <TextBlock Text="{Binding SelectedDisk.PartitionCount, StringFormat=Partitions: {0}}"/>
                        <TextBlock Text="{Binding SelectedDisk.DriveLettersDisplay, StringFormat=Drives: {0}, TargetNullValue=''}"/>
                        <TextBlock Text="{Binding SelectedDisk.ContainsOS, StringFormat=ContainsOS: {0}}"/>
                        <TextBlock Text="{Binding SelectedDisk.SystemDriveLetter, StringFormat=SystemDrive: {0}, TargetNullValue=''}"/>
                        <TextBlock Text="{Binding SelectedDisk.IsBootDisk, StringFormat=IsBootDisk: {0}}"/>
                        <TextBlock Text="{Binding SelectedDisk.HealthBadge, StringFormat=HealthBadge: {0}, TargetNullValue=''}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="0,12,0,0"
                   Opacity="0.8"
                   Text="{Binding StatusMessage}" />
    </Grid>
</UserControl>

