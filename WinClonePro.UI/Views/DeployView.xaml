<UserControl x:Class="WinClonePro.UI.Views.DeployView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:WinClonePro.UI.Views"
             mc:Ignorable="d"
             d:DesignHeight="850" d:DesignWidth="1100">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- SECTION 1 — WIM SELECTION -->
            <GroupBox Header="WIM selection" Grid.Row="0" Margin="0,0,0,12" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding WimPath, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,8,0"
                             materialDesign:HintAssist.Hint="WIM path"/>

                    <Button Grid.Column="1"
                            Content="Browse"
                            Command="{Binding BrowseWimCommand}"
                            Margin="0,0,8,0"/>

                    <Button Grid.Column="2"
                            Content="Load Images"
                            Command="{Binding LoadWimInfoCommand}"/>
                </Grid>
            </GroupBox>

            <!-- SECTION 2 — IMAGE LIST -->
            <GroupBox Header="Available images" Grid.Row="1" Margin="0,0,0,12" Padding="12">
                <DataGrid ItemsSource="{Binding AvailableImages}"
                          SelectedItem="{Binding SelectedImage, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          materialDesign:DataGridAssist.CellPadding="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="80"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding FriendlySize}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- SECTION 3 — TARGET DISK -->
            <GroupBox Header="Target disk" Grid.Row="2" Margin="0,0,0,12" Padding="12">
                <DataGrid ItemsSource="{Binding AvailableDisks}"
                          SelectedItem="{Binding SelectedTargetDisk, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          materialDesign:DataGridAssist.CellPadding="8">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBootDisk}" Value="True">
                                    <Setter Property="Background" Value="#55FFCDD2"/>
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Opacity" Value="0.8"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="80"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding FriendlySize}" Width="140"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding MediaType}" Width="120"/>
                        <DataGridTextColumn Header="Health" Binding="{Binding HealthBadge}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- SECTION 4 — DRIVER INJECTION -->
            <GroupBox Header="Driver injection (optional)" Grid.Row="3" Padding="12" Margin="0,0,0,12">
                <StackPanel Orientation="Vertical" Margin="0,0,0,12">
                    <CheckBox Content="Inject Drivers" IsChecked="{Binding InjectDrivers}" />
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding DriverFolderPath, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding InjectDrivers}"
                                 materialDesign:HintAssist.Hint="Driver folder path"/>
                        <Button Grid.Column="1"
                                Content="Browse"
                                Margin="8,0,0,0"
                                IsEnabled="{Binding InjectDrivers}"
                                Click="OnBrowseDriverFolderClicked"/>
                    </Grid>

                    <!-- SECTION 5 — DEPLOY BUTTON -->
                    <Button Content="DEPLOY IMAGE"
                            Height="54"
                            Width="240"
                            HorizontalAlignment="Left"
                            Background="#FFD32F2F"
                            Foreground="White"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            IsEnabled="{Binding CanDeploy}"
                            Command="{Binding StartDeployCommand}"/>
                </StackPanel>
            </GroupBox>

            <!-- SECTION 6 — PROGRESS -->
            <Border Grid.Row="4"
                    Margin="0,12,0,0"
                    Padding="12"
                    Background="{DynamicResource MaterialDesignPaper}"
                    CornerRadius="8"
                    Visibility="{Binding IsDeploying, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <ProgressBar Height="12"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding DeployProgress}"/>
                    <TextBlock Margin="0,8,0,0"
                               Text="{Binding StatusMessage}"/>
                    <TextBlock Margin="0,4,0,0"
                               Text="{Binding DeployProgress, StringFormat={}{0}%}"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>

