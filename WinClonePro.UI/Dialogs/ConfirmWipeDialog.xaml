<Window x:Class="WinClonePro.UI.Dialogs.ConfirmWipeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Confirm Disk Wipe"
        Height="360"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#22FFCDD2"
                BorderBrush="#FFFF1744"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10">
            <TextBlock FontWeight="Bold"
                       Foreground="#FFD50000"
                       Text="⚠ WARNING: THIS WILL ERASE THE DISK" />
        </Border>

        <StackPanel Grid.Row="1" Margin="0,14,0,0" >
            <TextBlock Text="ALL DATA on this disk will be permanently deleted." TextWrapping="Wrap"/>
            <TextBlock Margin="0,4,0,0" Text="This action CANNOT be undone." TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Disk Model:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding DiskModel, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,6,0,0" Text="Disk Size:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,6,0,0" Text="{Binding DiskFriendlySize, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,6,0,0" Text="Disk Index:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,6,0,0" Text="{Binding DiskIndex, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </Grid>

            <TextBox Margin="0,16,0,0"
                     materialDesign:HintAssist.Hint="Type WIPE to confirm"
                     Text="{Binding ConfirmationText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Width="120"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Margin="0,0,10,0" />

            <Button Width="140"
                    Content="Confirm"
                    Command="{Binding ConfirmCommand}"
                    IsDefault="True"
                    Background="#FFD32F2F"
                    Foreground="White" />
        </StackPanel>
    </Grid>
</Window>

