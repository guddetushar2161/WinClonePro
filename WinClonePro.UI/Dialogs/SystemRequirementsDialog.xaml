<UserControl x:Class="WinClonePro.UI.Dialogs.SystemRequirementsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="560">
    <Grid Margin="16">
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" CornerRadius="6" Background="#22FFCDD2" BorderBrush="#FFFF1744" BorderThickness="1">
            <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#FFD50000" Text="{Binding DialogTitle}"/>
        </Border>

        <ItemsControl Grid.Row="1" Margin="0,12,0,8" ItemsSource="{Binding RequirementRows}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   FontWeight="Bold"
                                   Foreground="{Binding StatusBrush}"
                                   Text="{Binding StatusText}"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding Label}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="2" Padding="12" CornerRadius="6" Background="{DynamicResource MaterialDesignPaper}">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Errors:" Margin="0,0,0,8"/>
                <ItemsControl ItemsSource="{Binding Errors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="#FFD50000" Text="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Install Windows ADK"
                    Margin="0,0,12,0"
                    Command="{Binding InstallAdkCommand}"
                    Visibility="{Binding ShowInstallAdkButton, Converter={StaticResource BoolToVis}}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="OK"
                    IsDefault="True"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>

