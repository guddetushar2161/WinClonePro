<Window x:Class="WinClonePro.UI.Dialogs.UsbSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Select USB Disk"
        Height="420"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Select a removable disk to create a bootable USB."/>

        <Border Grid.Row="1"
                Margin="0,10,0,10"
                Padding="10"
                CornerRadius="6"
                Background="#22FFCDD2"
                Visibility="{Binding IsBootDiskSelected, Converter={StaticResource BoolToVis}}">
            <TextBlock FontWeight="Bold" Foreground="#FFD50000" Text="Warning: selected disk appears to be the active boot disk."/>
        </Border>

        <Grid Grid.Row="2">
            <DataGrid ItemsSource="{Binding RemovableDisks}"
                      SelectedItem="{Binding SelectedDisk, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBootDisk}" Value="True">
                                <Setter Property="Background" Value="#55FFCDD2"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="80"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding FriendlySize}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Width="120" Content="Cancel" IsCancel="True" Command="{Binding CancelCommand}"/>
            <Button Width="140" Content="Confirm" Margin="12,0,0,0" Command="{Binding ConfirmCommand}" IsEnabled="{Binding CanConfirm}"/>
        </StackPanel>
    </Grid>
</Window>

